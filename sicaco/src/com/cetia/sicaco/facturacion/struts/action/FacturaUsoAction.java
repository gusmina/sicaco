/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.cetia.sicaco.facturacion.struts.action;

import java.util.HashMap;
import java.util.Map;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.struts.action.ActionErrors;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.hibernate.Transaction;
import org.jmesa.facade.TableFacade;
import org.jmesa.facade.TableFacadeImpl;
import org.jmesa.limit.ExportType;
import org.jmesa.limit.Limit;
import org.jmesa.view.component.Column;
import org.jmesa.view.component.Row;
import org.jmesa.view.component.Table;
import org.jmesa.view.editor.BasicCellEditor;
import org.jmesa.view.editor.CellEditor;
import org.jmesa.view.html.HtmlBuilder;

import com.cetia.sicaco.facturacion.struts.form.FacturaUsoForm;
import com.cetia.sicaco.hibernate.FacFusFacturaUso;
import com.cetia.sicaco.hibernate.FacFusFacturaUsoDAO;
import com.cetia.sicaco.struts.Constantes;
import com.cetia.sicaco.struts.DMLAction;

/** 
 * MyEclipse Struts
 * Creation date: 04-07-2008
 * 
 * XDoclet definition:
 * @struts.action path="/facturaUso" name="facturaUsoForm" input="redirectInvalidData" parameter="accion" scope="request" validate="true"
 * @struts.action-forward name="lista" path="pagina-lista.facturacion.facturaUso"
 */
public class FacturaUsoAction extends DMLAction {
	/*
	 * Generated Methods
	 */

	public static final String TABLA_ID = "facFusFacturaUso";
	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward lista(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		FacturaUsoForm facturaUsoForm = (FacturaUsoForm)form;
		facturaUsoForm.getFacturaUsoH().setFusNombre("");
		facturaUsoForm.getFacturaUsoH().setFusDescripcion("");
		FacFusFacturaUsoDAO facturaUsoDAO = new FacFusFacturaUsoDAO(getSessionHibernate(request));
		java.util.List lst = null;
		lst  = facturaUsoDAO.findAll();
		TableFacade tableFacade = new TableFacadeImpl(TABLA_ID, request);
		tableFacade.setItems(lst);
		//---- Genera los tipos de formas con que se podran exportar los datos
		tableFacade.setExportTypes(response, ExportType.CSV, ExportType.JEXCEL);//, ExportType.PDF);
		tableFacade.setStateAttr("restore");
		Limit limit = tableFacade.getLimit();
		if (limit.isExported()) {
        	//---- exporta la tabla
            export(tableFacade);
            return null; 
        } else {
        	//---- genera el html de la tabla para ser mostrada
            String html = html(tableFacade, request);
            request.setAttribute(Constantes.LISTA_KEY, html);
        }
        //----- Variables de configuracion
		request.setAttribute(Constantes.ACCION_KEY, "/facturaUso");
        request.setAttribute("filtro", "1");
		return mapping.findForward("lista");
		 
	}
	
	//---- Funcion que genera el html de la tabla del jmesa
	private String html(final TableFacade tableFacade, final HttpServletRequest request) {
		tableFacade.setColumnProperties("fusNombre","fusDescripcion","fusId");
		Table table = tableFacade.getTable();
		//---- Titulo de la tabla
		table.setCaptionKey("tbl.facturaUso.caption");
		
		Row row = table.getRow();
		Column nombreColumna = row.getColumn("fusNombre");
		nombreColumna.setTitleKey("tbl.facturaUso.fusNombre");
		
		nombreColumna = row.getColumn("fusDescripcion");
		nombreColumna.setTitleKey("tbl.facturaUso.fusDescripcion");
		
		nombreColumna = row.getColumn("fusId");
		nombreColumna.setTitleKey("tbl.facturaUso.eliminar");
		nombreColumna.getCellRenderer().setCellEditor(new CellEditor(){

			public Object getValue(Object item, String property, int rowcount) {
				Object value = new BasicCellEditor().getValue(item, property, rowcount);
				FacFusFacturaUso facturaUso = (FacFusFacturaUso)item;
				
				HtmlBuilder html = new HtmlBuilder();
				value = "eliminar";
				html.a().onclick("handlerDeleteButton("+ facturaUso.getFusId()+ ");").id("deleteButtonId").close();
				html.append(value);
				html.aEnd();
				
				return html.toString();
			}
		});
		
		return tableFacade.render();
	}
	
	//---- Funcion que genera los exports, el formato que tendran
	 private void export(final TableFacade tableFacade) {
		tableFacade.setColumnProperties("tprNombre","tprId");
		Table table = tableFacade.getTable();
		//---- Titulo de la tabla
		table.setCaptionKey("tbl.tipoProveedor.caption");
		
		Row row = table.getRow();
		Column nombreColumna = row.getColumn("tprNombre");
		nombreColumna.setTitleKey("tbl.tipoProveedor.tprNombre");
		
		tableFacade.render();
	}
	 
	public ActionForward guardar(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		FacturaUsoForm facturaUsoForm = (FacturaUsoForm)form;
		FacFusFacturaUsoDAO facturaUsoDAO = new FacFusFacturaUsoDAO(getSessionHibernate(request));
		Transaction tx = facturaUsoDAO.getSession().beginTransaction();
		try {
			facturaUsoDAO.save(facturaUsoForm.getFacturaUsoH());
			tx.commit();
		} catch (Exception e) {
			// TODO: handle exception
			tx.rollback();
			e.printStackTrace();
		}finally{
			facturaUsoDAO.getSession().flush();
			facturaUsoDAO.getSession().clear();
			
		}
		return lista(mapping,form,request,response);
	}

	public ActionForward eliminar(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		FacturaUsoForm facturaUsoForm = (FacturaUsoForm)form;
		FacFusFacturaUsoDAO facturaUsoDAO = new FacFusFacturaUsoDAO(getSessionHibernate(request));
		Transaction tx = facturaUsoDAO.getSession().beginTransaction();
		try {
			facturaUsoForm.setFacturaUsoH(facturaUsoDAO.findById(facturaUsoForm.getFusId()));
			facturaUsoDAO.delete(facturaUsoForm.getFacturaUsoH());
			tx.commit();
		} catch (Exception e) {
			// TODO: handle exception
			tx.rollback();
			e.printStackTrace();
		}finally{
			facturaUsoDAO.getSession().flush();
			facturaUsoDAO.getSession().clear();
			
		}
		return lista(mapping,form,request,response);
	}
	
	public ActionForward redirectInvalidData(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response){
		ActionErrors errors = (ActionErrors)request.getSession().getAttribute(Constantes.ERRORS);
		FacturaUsoForm facturaUsoForm = (FacturaUsoForm)form;
		facturaUsoForm.getFacturaUsoH().setFusNombre("");
		facturaUsoForm.getFacturaUsoH().setFusDescripcion("");
		FacFusFacturaUsoDAO facturaUsoDAO = new FacFusFacturaUsoDAO(getSessionHibernate(request));
		java.util.List lst = null;
		lst  = facturaUsoDAO.findAll();
		TableFacade tableFacade = new TableFacadeImpl(TABLA_ID, request);
		tableFacade.setItems(lst);
		//---- Genera los tipos de formas con que se podran exportar los datos
		tableFacade.setExportTypes(response, ExportType.CSV, ExportType.JEXCEL);//, ExportType.PDF);
		tableFacade.setStateAttr("restore");
		Limit limit = tableFacade.getLimit();
		if (limit.isExported()) {
        	//---- exporta la tabla
            export(tableFacade);
            return null; 
        } else {
        	//---- genera el html de la tabla para ser mostrada
            String html = html(tableFacade, request);
            request.setAttribute(Constantes.LISTA_KEY, html);
        }
        //----- Variables de configuracion
		request.setAttribute(Constantes.ACCION_KEY, "/facturaUso");
        request.setAttribute("filtro", "1");
		saveMessages(request, errors);
		return mapping.findForward("lista");
	}
	
	
	protected Map getKeyMethodMap() {
		HashMap<String, String> map = new HashMap<String, String>();
		map.put("cmd.facturaUso.lista", "lista");
		map.put("cmd.facturaUso.guardar", "guardar");
		map.put("cmd.facturaUso.eliminar", "eliminar");
		map.put("cmd.facturaUso.redirectInvalidData", "redirectInvalidData");
		return map;
	}
}